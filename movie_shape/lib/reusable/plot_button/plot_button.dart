import 'package:flutter/material.dart';

class PlotButton extends StatelessWidget {
  const PlotButton({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
